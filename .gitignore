# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/

# Environment
.env

# Raw source files and intermediate pipeline output (large / regenerable)
data/raw/
data/extracted/
data/chunks/
tmp/

# data/chroma/ is intentionally NOT ignored — the pre-built vector store
# is committed to the repo so the ingest pipeline never runs on the server.

# Test / IDE artefacts
.pytest_cache/
.DS_Store
*.swp
*.swo
.vscode/
.idea/
